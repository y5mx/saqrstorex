function getServiceOptions(elements){var options=[];var checked_options=[];elements.each(function(_elem){var _elem=$(this);if(_elem.is(':checkbox')&&_elem.is(':checked')){checked_options.push({'id':_elem.attr('data-option-id'),'value':_elem.val()})}
if(_elem.is(':selected')||(!_elem.is('option')&&!_elem.is(':checkbox')&&_elem.val())){options.push({'id':_elem.attr('data-option-id'),'value':_elem.val()});}});checked_options.forEach(function(item){var existing=options.filter(function(v){return v.id===item.id;});if(existing.length){var existingIndex=options.indexOf(existing[0]);options[existingIndex].value=options[existingIndex].value.concat(item.value);}else{if(typeof item.value=='string')
item.value=[item.value];options.push(item);}});return options}
function getDefaultOptions(formData){$('.prod-options-list').each(function(){if($(this).is(':radio')&&$(this).is(':checked')){formData.append('options[]',$(this).val());}else if(!$(this).is(':radio')&&$(this).val()){formData.append('options[]',$(this).val());}});return formData}